package SVM;

import junit.framework.TestCase;

/**
 * Created by kp on 16/9/12.
 */
public class SVM extends TestCase{

    /**
     * 支持向量机的基本公式：max[∑(i=1,m)a - 1/2*∑（i,j=1,m）label(i)*label(j)*a(i)*a(j)*<x(i)*x(j)>]
     *s.t a>=0,∑(i=1,m)a(i)*label(i) = 0
     * **/
    public void testSVM(){

    }
}
